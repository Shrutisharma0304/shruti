#include <stdio.h>

int main() {
    int n, original, lastDigit, firstDigit;
    int numDigits, swapped;


    printf("Enter a positive integer: ");
    scanf("%d", &n);

    if (n < 0) {
        printf("Please enter a non-negative integer.\n");
        return 0;
    }

    original = n;

    if (n < 10) {
        printf("After swapping first and last digit: %d\n", n);
        return 0;
    }

  
    
